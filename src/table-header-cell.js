import TableHeaderCell from '@ui5/webcomponents/dist/TableHeaderCell.js';

export default class HTableHeaderCell extends TableHeaderCell {
  constructor() {
    super();
  }
}

customElements.define('h-table-header-cell', HTableHeaderCell);
