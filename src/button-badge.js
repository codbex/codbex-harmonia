import ButtonBadge from '@ui5/webcomponents/dist/ButtonBadge.js';

export default class HButtonBadge extends ButtonBadge {
  constructor() {
    super();
  }
}

customElements.define('h-button-badge', HButtonBadge);
