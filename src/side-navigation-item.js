import SideNavigationItem from '@ui5/webcomponents-fiori/dist/SideNavigationItem.js';

export default class HSideNavigationItem extends SideNavigationItem {
  constructor() {
    super();
  }
}

customElements.define('h-side-navigation-item', HSideNavigationItem);
