import SideNavigationGroup from '@ui5/webcomponents-fiori/dist/SideNavigationGroup.js';

export default class HSideNavigationGroup extends SideNavigationGroup {
  constructor() {
    super();
  }
}

customElements.define('h-side-navigation-group', HSideNavigationGroup);
