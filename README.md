# codbex-harmonia

UI Components Library for Vue

After cloning the repository:

        npm install

Build the UMD distribution file:

        npm run build

Run the sample page:

        npm run dev

Open the browser at: http://localhost:5173/
