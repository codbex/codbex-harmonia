---
outline: deep
---

# Utility classes

Harmonia UI uses TailwindCSS under the hood, so the provided CSS utility classes either directly map to those in Tailiwnd or are very similar in name and function.
